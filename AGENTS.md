# Reglas del Proyecto

- **Sin navegador para pruebas**: NO abras el navegador Chrome ni uses el subagente de navegador (`browser_subagent`) para probar cambios automáticamente, a menos que se solicite de forma explícita.
